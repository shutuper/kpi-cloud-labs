spring:
  application:
    name: spring-app

  datasource:
    url: jdbc:postgresql://192.168.56.1:6432/testdb
    username: testuser
    password: TestUs3r4DB
  jpa:
    show-sql: ${jpa.show-sql:false}
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

